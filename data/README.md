# Local data directory

This directory is ignored by Git except for this file. It may be used for synthetic examples. Do not place controlled clinical data inside a cloned public repository; prefer an institutionally managed path outside the repository and pass it with `--data` or `ATAAD_POP_DATA`.

